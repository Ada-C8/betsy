<h2>Add new Category/h2>

<%#= render partial: 'layouts/error_messages',
    locals: { model: @work } %>
<h3>Category Name Limit: 30 Characters</h3>

<%= form_for @category do |f| %>
  <%= f.label "Name" %>
  <%= f.text_field :category_name %>

  <%= f.submit "Create Category", :class=>"button"  %>

<% end %>
