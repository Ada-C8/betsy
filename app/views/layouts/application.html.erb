<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="icon" href="https://lh6.ggpht.com/7cfXEzMehr9OvKbOyL1p3c454kWKEmBON9CJ1NvhZFv-cJY0aTnX7pKBTG8vL-oQOQ=w300">


    <title><%= content_for?(:title) ? yield(:title) : "Sourceress" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <!-- <%= csrf_meta_tags %> -->

    <link href="https://fonts.googleapis.com/css?family=Josefin+Slab" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Berkshire+Swash" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Cinzel" rel="stylesheet">

    <%= yield :head %>
  </head>

  <body>

    <header class="header navigation">
      <span class="logo"><%= link_to "Sourceress", root_path %></span>
      <section class="dropdown">
        <ul class "view-menu">
          <button class="dropdown-button-view">Browse</button>
            <div class="dropdown-content-view">
              <li><%= link_to "Browse Products", products_path %></li>
              <li><%= link_to "Browse Merchants", merchants_path %></li>
              <li><%= link_to "Browse Categories", categories_path %></li>
            </div>
        </ul>
      </section>
      <section class="dropdown">
        <ul class="create-menu">

          <button class="dropdown-button-create">
            <% if session[:order_id] == nil %>
              <%= link_to "Shopping Cart", orders_path, method: :post %>
            <% else %>
              <%= link_to "Shopping Cart", order_path(session[:order_id]) %>
            <% end %>


          </button>

            <div class="dropdown-content-create">
              <li></li>
              <li></li>
            </div>
        </ul>
      </section>
      <section class="dropdown">
        <ul class="create-menu">
          <button class="dropdown-button-create">Login</button>
            <div class="dropdown-content-create">
              <li></li>
              <li></li>
            </div>
        </ul>
      </section>
      </span>
    </header>


  <% if flash[:result_text] or flash[:messages] %>
    <section class="row status <%= flash[:status] %>">
      <h3><%= flash[:status] == :failure ? "A problem occurred: " : "" %><%= flash[:result_text] %></h3>
      <% if flash[:messages] %>
        <ul>
          <% flash[:messages].each do |name, problems| %>
            <% problems.each do |problem| %>
              <li><%= name %>: <%= problem %></li>
            <% end %>
          <% end %>
        </ul>
      <% end %>
    </section>
  <% end %>

    <%= yield %>

  </body>
</html>
