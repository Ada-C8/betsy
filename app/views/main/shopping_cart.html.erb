<section class="view-cart">
  <h2 class="page-title">Shopping Cart</h2>

  <% @products.each do |order_product| %>
    <p><%= order_product.product.name %></p>
    <p><%= order_product.total %></p>
  <% end %>

  <p>cart total</p>
  <%= total(@products)%>

</section>
